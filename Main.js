import Nethereum.HdWallet;

string = "radar blur sun chef fix engine embark joy scheme fiction master release";
wallet = new Wallet(words, null);
Console.WriteLine(Wallet.GetAccount(0).Address)

// wallet = new Wallet(Wordlist.English, WordCount.Twelve);

// Console.WriteLine("Addresses:");
// for(int i=0, i<20, i++)
